package com.zxxf.assistant.ui.chat.components

/**
 * Catppuccin-themed Markdown table components.
 *
 * NOTE: Full cell-level customization (borders, header background, zebra stripes,
 * horizontal scroll) requires mikepenz >= 0.39.x which depends on Kotlin 2.3.x.
 * In 0.38.0 (current, compatible with Kotlin 2.2.10), we rely on the library's
 * default table rendering plus the `markdownColor(tableBackground = ...)` color hint.
 *
 * TODO: When upgrading to Kotlin 2.3.x, upgrade mikepenz to >= 0.39.x and enable
 * the `tableHeader`, `tableRow`, `tableCell` component overrides for full styling.
 */
